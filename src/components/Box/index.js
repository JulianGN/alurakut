import styled from 'styled-components';

// sempre que for um componente, começa maiúsculo
const Box = styled.div`
  background: #ffffff;
  border-radius: 8px;
  padding: 16px;
`; 

export default Box;